# To Do

- update the to do file(!)
- Unify error handling in home/auth: some methods use callbacks, some methods use { data, error }. Make them all use { data, error }. Also, add toaster messages for errors/info/success where missing.
